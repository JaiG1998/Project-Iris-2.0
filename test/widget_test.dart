import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Dummy test for IRIS compilation', () {
    // This is a placeholder test that always passes to keep the compiler happy
    expect(true, isTrue);
  });
}